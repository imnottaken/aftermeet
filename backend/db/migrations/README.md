# Database Migrations

This directory contains the SQL migrations for the AfterMeet MVP.

## Migration Notes

- `0001_initial_schema.sql` creates the core meeting intelligence tables.
- The schema is intentionally simple and assumes a single-user-context MVP.
- Run these migrations in Supabase SQL Editor, your preferred migration runner, or through a future migration tool.

